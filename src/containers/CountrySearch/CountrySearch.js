import React, { Component } from 'react';

export default class CountrySearch extends Component {
    render() {
        return (
            <h1>This is country search component</h1>
        );
    }
}